# 진행 현황 (ai-dt.net)

최종 갱신: 2026-08-18

## 남은 작업 (핸드오프)

> GitHub: `eduinside/ai-dt.net`. 현재 작업 브랜치 **`2608`**(개선사항, main 미병합).
> `npm install && npm run dev`로 이어서 작업. 빌드 18페이지 정상, 내부 링크 broken 0.

**운영자 콘텐츠 채우기 (2608 브랜치)**
- [ ] 수업이야기 카드뉴스: `src/data/classCards.ts`에 항목 추가 + `src/assets/cards/{case|howto|edtech}/{id}/01.png…`
- [ ] 활용 수업 사례 PDF: `public/pdf/case/`에 파일을 두고 항목의 `pdf` 값에 파일명 기입
- [ ] 홈 카드뉴스 교체·추가 시 `src/assets/pages/index/cardnews/` (16:9 권장) — 2026-08-12부터 홈이 아닌 `/index/AIDT`(활용 도움자료) 페이지 제목 바로 아래로 이동.
- [x] 홈 상단 홍보 영상 추가(2026-08-11, 2026-08-12 보완): `src/pages/index.astro`, `edu-link` 워커의 `/media/*` 프록시로 R2(`edulink-pages-media` 버킷) 객체를 서빙 — `https://dgedu.link/media/ai-dt-net/202608_promo.mp4`. 음소거·자동재생·무한반복(loop), 클릭 시 네이티브 재생 컨트롤 토글. 모바일은 좌우 여백 없이 풀블리드, 데스크톱은 하단 CTA 패널(`max-w-6xl`)과 동일 폭. ai-dt.net은 R2 직접 바인딩 없음.

**배포 (P5, 운영자 계정 필요)**
- [ ] Cloudflare Pages ↔ GitHub 레포 연동 (설정값·절차: `docs/DEPLOY.md`). 운영자가 진행 예정.
- [ ] `*.pages.dev` 프리뷰 검수 → 관계자 확인.
- [ ] ai-dt.net 커스텀 도메인 연결 + DNS 전환(TTL 낮추고, 롤백 경로 확보). 구글 사이트는 전환까지 유지.

**콘텐츠 채우기 (운영자)**
- [ ] 인증 걸린 원본 이미지 드롭인: `src/assets/pages/{manual/1-neis, manual/2-signup, eduonepass/signup}/`에 `01.png…` 저장하면 자동 표시. 안내: `src/assets/pages/README.md`.
- [ ] 발행사 답변 12곳·FAQ 문서가 최신인지 확인(원본 갱신 시 `src/assets/replies/…` 교체, 스키마 `src/content/publishers/*.mdx`).

**P4 잔여 (기능 마감)**
- [x] 사이트 내 검색(Pagefind) 도입 — 헤더 🔍 → `/search`, 18페이지 인덱싱 확인(2026-07-28).
- [ ] 접근성(KWCAG) 점검 — 대비·포커스·대체텍스트·landmark 최종 확인.
- [ ] 동료용 저작 양식/가이드 문서(P4 산출물) — `docs/`에 마크다운 템플릿 + 메타 항목표.
- [ ] (선택) 웹폰트 self-host 전환 — 현재 CDN(Pretendard/Hahmlet). 개인정보 원칙상 P4에서 전환 가능.

**보류·확인 사항**
- manual·eduonepass "가입 방법 영상"은 원본이 인증 걸린 구글 자산이라 자동 이관 불가 → 이미지 드롭인으로 대체하거나, 유튜브 원본이 있으면 `<YouTube id=…/>`로 임베드 가능.
- `docs/original/CONTENT.md`(원본 실측)·`replies-crawl.json`(이미지 인벤토리)는 재크롤 없이 참고용으로 보존.

---

## 현재 단계: 개선사항 (브랜치 `2608`)

### 수업이야기 콘텐츠 등록·정렬·검색 딥링크 (2026-08-18)

**1. 신규 오픈 배너 (랜딩)**
- 학습데이터 분석·활용 포털 오픈 안내 + 수업 사례 추가 안내, 배너 2건 추가.

**2. `class/case`·`class/edtech` 카드뉴스 콘텐츠 등록**
- `case` 12건(수업안 PDF 포함)·`edtech` 19건 등록(`src/data/classCards.ts` + `src/assets/cards/{case|edtech}/{id}/`).
- 카드에 발행사·과목·학년학기·도구 태그, 도구 링크(더 알아보기) 표시(`src/data/toolLinks.ts`).
- 라이트박스를 좌(이미지)·우(제목·태그·설명·PDF) 2단 레이아웃으로 재구성, 모바일 대응. 카드뉴스 비율 1688:2110.
- `class/howto` 네비게이션 숨김(콘텐츠 없음), `case`/`edtech`에 NEW 배지 추가.
- 팀 전달용 `docs/class-cards-guide.md`·`docs/class-cards-template.csv` 추가.

**3. `class/case`·`class/edtech` 카드 정렬 기준 변경 (금일)**
- `class/case`: 대분류(초·중·고, 학교급)는 유지, 각 대분류 안에서 **1차 교과(수학→영어→정보) →
  2차 학년학기(오름차순)**로 정렬(기존에는 과목별로만 묶여 있었음).
- `class/edtech`: 대분류를 학교급(초·중·고) → **교과(수학 → 영어 → 정보)**로 전환, 각 대분류 안에서
  **1차 학교급(초→중→고) → 2차 도구 이름**으로 정렬.
- `CardNewsGallery.astro`에 `groupBy` prop('level'|'subject')과 `compareBy()` 정렬 헬퍼를 추가,
  `classCards.ts`에 `subjectFamilyOf()`(subject 접미사로 수학/영어/정보 판정)·`subjectFamilyOrder`를
  신설 — 그룹핑·정렬은 렌더링 시 계산(데이터 배열 물리적 순서와 무관).
- 라이트박스 데스크톱 좌우 네비게이션 버튼을 반투명 흰 배경(`bg-white/10`)에서 검은 반투명 배경 +
  흰 테두리 + 그림자(`bg-black/40`, `border-white/40`, shadow, backdrop-blur)로 교체 — 밝은 카드뉴스
  이미지 위에서 대비가 약해 잘 안 보이던 문제 수정.

**4. 검색(Pagefind) 개선**
- `/search` 진입 시 검색창 자동 포커스, 지우기 버튼 고정폭으로 인한 줄바꿈 깨짐 수정.
- 카드 제목을 `id` 붙은 `h3`로 변경 → Pagefind sub-result가 카드별 제목·딥링크를 생성,
  `#카드id`로 접속하면 해당 카드 라이트박스가 자동으로 열림.

### 포털 명칭 변경·문의 페이지 재구성·랜딩 정리 (2026-08-12)

**0. 포털 명칭 전면 변경 (사이트 전체)**
- "AI·디지털 교육자료 포털" → **"학습데이터 분석·활용 포털"**, `www.aidtbook.kr` → **`www.edupath.kr`**.
  헤더/랜딩/푸터/사전 준비사항 안내/활용 도움자료/스스로 학습 안내/문의 페이지 등 텍스트·링크 전량 치환.
- `/study` 페이지의 포털 바로가기 버튼 이미지(`src/assets/pages/study/portal-button.png`)도 새 이름·URL로
  다시 생성(`sharp` + SVG 래스터라이즈, pptx에서 추출한 손가락 아이콘 재사용).

**1. 발행사 목록 변경** (`AI·디지털 교육자료 발행사 공동 CS 센터` ☎1661-0777)
- 참여사 11개사 → **8개사**로 축소: 교문사·도서출판 길벗·동아출판·씨마스·아이스크림미디어·
  **엘리스그룹**(신규)·와이비엠·**잇플**(개별→참여사로 이동). 클래스팅은 목록에서 제외,
  디딤돌교육·비상교육·엔이능률·천재교과서·천재교육은 개별 지원 창구로 이동.
- 개별 출판사 지원 창구(`AI·디지털 교육자료 발행사별 지원 센터`)는 9개사로 재구성, 전화번호 갱신
  (미래엔 02-3475-4021, 금성출판사 02-2077-8233, 지학사/교학사/디딤돌교육 1566-1053 공유,
  비상교육 1544-0554, 천재교육/천재교과서 1522-1955 공유, 엔이능률 1833-8368).
- 랜딩 히어로 카드·푸터의 "11개 발행사" 표기 → "8개 발행사"로 갱신.

**2. `/contact` 카드 순서·내용 재구성**
- 순서: 학습데이터 분석·활용 포털 문의(대처요령 박스 포함) → 회원가입 관련 문의 → 대구광역시교육청
  테크센터 운영 → 발행사 공동CS → 개별 출판사.
- "문의 발생시 대처요령"(pptx 1쪽 내용: 스마트문의등록→답변 확인 2단계)을 별도 카드가 아니라
  **학습데이터 분석·활용 포털 문의 카드 안에 내장**. 제목을
  "학습데이터 분석·활용 포털에서 [스마트문의]로 쉽게 문의하고 답변 확인하기"로 정리.
- 회원가입 관련 문의 카드에 지원 내용/접수·처리 내용 추가(`/index/eduonepass` 페이지 내용 참조),
  링크 텍스트를 "교육디지털원패스(edupass.neisplus.kr)"로 URL 명시.
- 대구광역시교육청 테크센터 운영 카드 제목 옆에 전화번호(☎ 053-235-9999) 표기 추가.
- 전화번호는 전부 `☎`, 외부/페이지 링크 뒤에는 공통 우상향 화살표 아이콘
  (신규 `src/components/ArrowIcon.astro`)을 붙이도록 통일. 좁은 화면(300~375px)에서
  텍스트 겹침·overflow 없음을 실측 확인.

**3. 랜딩 정리**
- 카드뉴스 섹션을 홈에서 제거하고 `/index/AIDT`(활용 도움자료) 페이지 제목 바로 아래로 이동
  (제목 없이, 원래 폭 그대로).
- 홍보 영상: 모바일은 좌우 여백 없는 풀블리드, 데스크톱은 하단 CTA 패널과 동일 폭(`max-w-6xl`).
  클릭하면 네이티브 재생 컨트롤이 토글되도록 스크립트 추가.

**4. `/class/curriculum` 자료 링크**
- "디지털 역량 교육자료(KERIS)" 카드의 초등/중등 자료 버튼이 자리표시자(`#`)였던 것을 실제
  KERIS 링크로 연결.

### 랜딩·문의·수업이야기 개선 (2026-07-28) — 계획서: `docs/PLAN-2608.md`

**0. 분기 프리뷰 유지**
- `BaseLayout`의 pages.dev → 커스텀 도메인 이전 스크립트가 `.pages.dev`로 끝나는 **모든** 호스트를 넘겨
  분기 프리뷰까지 튕기던 문제 → 정본 호스트(`ai-dt-net.pages.dev`) 정확 일치일 때만 이전하도록 축소.

**1. 랜딩**
- 선도학교 FAQ 섹션 + `활용 도움자료 더 보기` 버튼 제거 → **16:9 카드뉴스**(자동 넘김 5초, 5장 게시).
- 히어로 연락처 카드에 `[AI·디지털 교육자료 11개 발행사] 공동 CS 센터 1661-0777` 추가.
  전화번호는 노출하되 **3개 카드 모두 `/contact`로 연결**(전화 걸기는 문의 페이지에서).

**2. 문의 페이지**
- 하단 안내 이미지 카드 → **공동CS 센터(참여사 11곳)** / **발행사 지원 센터(미참여사 6곳)** 두 카드로 구조화.
  지원 내용·접수/처리 내용을 2단 계층 목록으로 정리. 데이터 배열(`desks`) 하나로 두 카드를 렌더.

**3. 수업이야기 신규 3개 페이지** (`/class/case`, `/class/howto`, `/class/edtech`)
- 대분류(초·중·고) + 데스크톱 3열 갤러리(1:1 썸네일 + 제목) → 라이트박스 카드뉴스.
  `case`는 라이트박스 하단 PDF 버튼(파일 없으면 준비 중 안내).
- 목록·제목은 `src/data/classCards.ts`, 이미지는 `src/assets/cards/{섹션}/{id}/`.

**공통화 (기존 요소에도 적용)**
- `src/data/nav.ts` — 헤더·사이트맵·수업이야기 허브가 같은 메뉴 데이터를 사용(이전에는 3벌 복붙).
- `SlideDeck` — 폴더 glob + 캐러셀 + 플레이스홀더 일체. 홈 카드뉴스와 **기존 학습지원 SW 페이지**가 공유.
- `Lightbox` — 트리거가 이미지 목록을 넘기는 방식. 갤러리와 **기존 study 페이지**가 공유.
- `Carousel`에 `autoplay`/`interval`/`ratio`, `srcset` 지원 추가. 위치 계산을 스크롤 좌표 기준으로 교정
  (IntersectionObserver 콜백 지연 시 같은 슬라이드에 머무는 문제).

**4. 남은 작업 처리**
- `_redirects`의 `/class/중등-…` 대상이 존재하지 않는 `/class/secondary`였음 → `/class/mid`로 수정.
- 검색 페이지: Pagefind 커스텀 CSS가 **구 디자인(딥 틸/코랄) 하드코딩** → 현재 네이비 토큰으로 교체.
  이때 두 가지 실제 결함을 함께 수정 —
  ① 변수와 규칙을 `:root`/`.pagefind-ui`에 선언해 나중에 로드되는 `pagefind-ui.css`에 덮여 **적용되지 않던 문제**
     → `#search` 컨테이너로 스코프 이동, ② 번역 자리표시자를 `[search]`/`[count]`로 적어 결과 메시지가
     `"[search]에 대한 [count]개의 검색 결과"`로 **원문 노출되던 문제** → `[SEARCH_TERM]`/`[COUNT]` 규격으로 교정.
- 검색 결과 없을 때를 대비한 주요 페이지 바로가기 칩 추가, 사이트맵에 `그 밖의 페이지`(홈·검색·사이트맵) 묶음 추가.
- 이미지 최적화: 홈 카드뉴스 PNG 5장 5.6MB → WebP(q85) 1600w 합계 약 310KB.

## 이전 단계: 리디자인 — 모던 공공 포털

### 전면 리디자인 + 피드백 반영 (2026-07-11)
- **디자인 방향**: "구글 사이트 느낌" 탈피 → **모던 공공 포털**(딥 네이비 + 웜 오렌지 액센트). 토큰·레이아웃 문법은 `docs/DESIGN.md`에 재사용 가능하게 문서화.
- **토큰 교체**(`src/styles/global.css`): 스틸 슬레이트·파스텔 시안 → 네이비 스케일(primary-50~800) + `accent-soft`. 구 토큰명(`hero-*`, `band`)은 값만 바꿔 하위 페이지 호환 유지.
- **홈**: 중앙 정렬 히어로 → 비대칭 2열(좌 타이틀 / 우 연락처 전화 카드 — 구 연회색 밴드 흡수, 설명문은 "활용하여" 뒤 줄바꿈). 다크 밴드 2개 해체(절차 문구 → 섹션 헤딩, 문의 → 네이비 CTA 패널). Step 카드 → 번호 레일(01·02 + 연결선, 3번째는 "추천자료" 배지). UI 라벨 이모지 제거(본문 문구는 원본 유지).
- **공통**: 푸터 다크 네이비 + 우상단 교육청 로고(64px, 원본 webp가 이미 투명 배경), 헤더 활성 메뉴 필 스타일, `title-underline` 듀오톤 마크로 재정의(하위 페이지 자동 적용), LinkButton `rounded-lg`.
- **발행사 답변**: 정적 상세 페이지(`[id].astro`) 삭제 — 허브의 클라이언트 필터(아코디언 제자리 표시)로 단일화. 삭제된 페이지 참조 링크 없음 확인. ※ PowerShell `Remove-Item`은 `[id]`를 와일드카드로 해석하므로 `-LiteralPath` 필요.
- **콘텐츠 수정**: 금성출판사 회신 이미지 6번(`replies/kumsung/01/06.png`) 삭제(문서 5장). 수업설계 안내 문구(ℹ️/※) → info 아이콘 콜아웃 카드로 정리.
- 빌드 27페이지 정상(발행사 상세 12페이지 제거 반영), 콘솔 오류 0. 데스크톱·모바일 프리뷰 확인.

## 이전 단계: P3~P5 준비 — 핵심 완료

### 피드백 필터·이미지 슬롯·배포 준비 (2026-07-10)
- **발행사 답변**: "전체" 칩 제거, 초기 목록 숨김 → 발행사 이름 클릭 시 **페이지 이동 없이 클라이언트 필터**(칩 강조, #슬러그 딥링크, no-JS 폴백).
- **manual 빈 아코디언 제거**(빈 확장 UI 정리). 본문 텍스트·체크리스트·링크로 완결.
- **인증 걸린 원본 이미지 대응**: manual·eduonepass 단계 스크린샷은 학교 구글 계정 인증이 걸려 자동 이관 불가(403/CORS). → `PageImages` 컴포넌트 + 드롭 폴더(`src/assets/pages/{manual/1-neis,manual/2-signup,eduonepass/signup}/`) 배치. 운영자가 파일을 넣으면 자동 표시, 없으면 안내 플레이스홀더. 방법은 `src/assets/pages/README.md`.
- **배포 준비(P5)**: GitHub → Cloudflare Pages 방식 확정. `.nvmrc`(Node 20) + `docs/DEPLOY.md`(빌드 설정·커스텀 도메인 절차). 실제 레포 생성·CF 연결·DNS 전환은 운영자 계정 필요.
- 유튜브 영상(중등·수업설계)은 정상 렌더 확인.

## 이전 단계: P3~P4 (전 페이지 이관 + 마감)

### 접속 무결성 · 용어 변경 (2026-07-10)
- **용어 변경**: 사용자 대상 표기 "출판사" → **"발행사"**, 허브 제목 "출판사 피드백" → **"발행사 답변"** (헤더 메뉴·푸터·피드백/수업설계 페이지·404). 회사 고유명(금성출판사)·영문 코드 식별자(`publishers`)는 유지.
- **접속 안 되는 페이지 0 확인**: 빌드 산출물의 내부 링크를 전수 점검(broken=0). 헤더 "지원센터"가 가리키던 `/index`가 페이지 부재로 404였던 문제 → 원본과 동일하게 `/index` → `/` 정적 리다이렉트(astro.config) 추가.
- **404 페이지**(`src/pages/404.astro`): 잘못된 주소도 헤더 네비 + 주요 바로가기 제공(Cloudflare Pages가 dist/404.html 자동 사용).
- OG 메타·sitemap·favicon은 기존 반영. (검색(Pagefind)은 P4 잔여.)

### P3 완료 (2026-07-10)
- **일반 페이지 전량 이관** (원본 CONTENT.md 텍스트·구성요소 그대로):
  - `/index/manual` 사전 준비사항 안내(4단계 + 교육부 PDF·Canva·HWP 링크), `/index/AIDT` 활용 도움자료, `/index/eduonepass` 교육디지털원패스(서비스 6종), `/index/software` 학습지원 소프트웨어.
  - `/class` 수업이야기 허브(튜터링·교사/학생/학부모·웹전시관), `/class/curriculum` 수업설계 지원(KERIS·에듀나비 링크 + 단원/영역 목록 + 활용전략 영상), `/class/secondary` 중등 지원(유튜브 6편).
  - `/study` 스스로 학습 안내(원본 안내 포스터 이관 + 포털 바로가기 버튼), `/contact` 문의 및 장애 지원.
- **홈 FAQ**: GAS 임베드 → 정적 게시판 이관, `/index/faq`와 데이터 공유(src/data/faq.ts, 게시일 2026-05-03).
- **한글 경로 → 영문 slug** `public/_redirects` (수업이야기 하위 + 출판사 12곳).
- 외부 링크·유튜브는 유지(C6). 빌드 24페이지 정상, 콘솔 오류 0.

### 이미지 이관 참고 (Google Sites 토큰)
- googleusercontent 이미지 URL 토큰은 **페이지 로드마다 회전**하고 서명에 크기 파라미터가 포함됨 → 크롤 직후 즉시 다운로드해야 하며, DOM의 실제 `src`(크기 접미사 포함)를 그대로 사용해야 함(회신 이미지는 `=w1280`, 포스터는 원본 접미사).
- software 포스터는 carousel 지연 로드로 안정적 캡처 실패 → 텍스트+에듀집 링크로 충분(장식 이미지 생략). manual·eduonepass 본문 스크린샷도 텍스트가 내용을 담고 있어 생략.

## 이전 단계 기록: P2 (반복 콘텐츠 이관)

### P2 완료 (2026-07-10)
- **출판사 피드백 → QnA형 게시판(필터형)으로 재구성** (관계자 요청 반영):
  - 허브(`/index/feedback`): 원본 제목·안내문 + 출판사 필터 칩 + 전체 회신 목록(출판사 배지 + 교과 + 문서 수, 펼치면 문서 이미지).
  - 상세(`/index/feedback/{slug}`): `'{이름}' 회신 내용` 제목 + 해당 출판사만 목록형 필터 + ◀ 돌아가기. 필터는 정적 페이지 링크(JS 0줄).
  - **회신 문서 이미지 109장 전량 이관** → `src/assets/replies/{slug}/{섹션번호}/`. 빌드 시 WebP 최적화(예: 592KB→92KB). 원본 URL 인벤토리는 `scripts/download-replies.mjs`에 보존(서명상 `=w1280`만 유효).
- **`/index/faq` 페이지**: 원본 아코디언 3항목 → 같은 게시판 스타일, 문서 이미지 7장 이관.
- **홈 FAQ 임베드**: 원본 GAS(QnA 게시판) iframe 유지 임베드 — 프리뷰에서 정상 로드 확인.
- **로고·파비콘**: 원본 '교육수도 대구' 로고(2048px PNG) 확보 → 파비콘/애플터치 아이콘/헤더/히어로 반영 (`src/assets/logo.png`).
- **모바일 대응**: 헤더 햄버거 메뉴(`<details>` 기반, JS 0줄) + 게시판·그리드 반응형. 모바일(375px) 프리뷰 확인 완료.
- 빌드 15페이지 정상, 콘솔 오류 0.

### 남은 P2 잔여
- `resources` 컬렉션(매뉴얼·소프트웨어·도움자료 카드) 이관 — /index/manual·AIDT·software 페이지(P3)와 함께 진행이 효율적.

## 이전 단계 기록: P1 (골격 + 디자인 시안)

### 방향 전환 (2026-07-09 검수)
- **원본 충실 재현 원칙 확정**: 원본(구글 사이트)의 히어로 등 모든 텍스트는 사전 협의된 것 → 그대로 사용, 부가 텍스트·요소 삽입 최소화. 키 색상·메뉴 구조·메뉴 내 콘텐츠도 최대한 유지.
- 원본 전 페이지 실측 크롤링 완료 → `docs/original/CONTENT.md` (텍스트 전문, 네비 구조, 키 색상 `#263840`, 임베드 인벤토리).
- 초기 시안(딥 틸/코랄, 임의 히어로 문구, CTA 카드)은 원본 기준으로 전면 재작업.

### 완료
- Astro 5 + Tailwind 4 + MDX + sitemap 프로젝트 골격 (`npm run build` 정상, 15개 페이지 생성)
- 디자인 토큰 확정: 종이빛 배경 · 딥 틸(주색 `#0f7d84`) · 웜 코랄(강조 `#e2623b`), 제목 Hahmlet(한글 세리프) / 본문 Pretendard
- 재사용 컴포넌트 = 디자인 시스템: Header, Footer, PublisherCard, LinkButton, SectionHeading, **Accordion**(`<details>` 기반), **Carousel**(scroll-snap)
- 콘텐츠 컬렉션 스키마(publishers/pages/resources/faq) — zod 검증
- 시안 3종 렌더 확인:
  - 홈 (`/`) — 히어로 + 바로가기 카드 4 + 출판사 프리뷰
  - 출판사 허브 (`/index/feedback`) — 13개 카드 + 교과 칩
  - 출판사 상세 (`/index/feedback/[id]`) — 아코디언 + 유튜브 + CTA (천재교과서·비상교육 상세 본문 포함)

### 남은 P1 작업
- **Cloudflare Pages 연결** — GitHub 레포 생성 + CF Pages 프로젝트 연결(운영자 계정 필요). 빌드 명령 `astro build`, 출력 `dist/`.
- `*.pages.dev` 프리뷰 링크로 관계자 디자인 검수 → 색/타이포/컴포넌트 확정.

### 검수 결정 확정 (2026-07-09)
- 웹폰트: **CDN 유지, P4에서 self-host 전환**(콘텐츠 이관 먼저 마무리).
- 2depth 경로: **영문 slug 유지**(`/index/feedback/cheonjae`) — 기존 12개 데이터 파일과 일치, 추가 작업 없음.
- 다크 모드: **미지원, 라이트 전용 유지**.

## 다음 단계
- P2: `resources`·`faq` 이관 + 출판사 12곳 실제 링크/자료 채우기.
- P3: 일반 페이지(홈·안내·수업이야기·스스로학습·문의) 이관 + 구글 임베드 전수 점검.

## 알려진 참고사항
- 로컬 스크린샷 도구는 Pretendard 동적 서브셋(파일 ~100개)의 networkidle 대기로 간헐 지연됨 — 사이트 결함 아님. 실제 브라우저는 정상.
