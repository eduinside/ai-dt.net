# 진행 현황 (ai-dt.net)

최종 갱신: 2026-07-11

## 남은 작업 (핸드오프)

> GitHub: `eduinside/ai-dt.net` (main). 로컬·원격 동기화됨. `npm install && npm run dev`로 이어서 작업.
> 빌드 25페이지 정상, 내부 링크 broken 0.

**배포 (P5, 운영자 계정 필요)**
- [ ] Cloudflare Pages ↔ GitHub 레포 연동 (설정값·절차: `docs/DEPLOY.md`). 운영자가 진행 예정.
- [ ] `*.pages.dev` 프리뷰 검수 → 관계자 확인.
- [ ] ai-dt.net 커스텀 도메인 연결 + DNS 전환(TTL 낮추고, 롤백 경로 확보). 구글 사이트는 전환까지 유지.

**콘텐츠 채우기 (운영자)**
- [ ] 인증 걸린 원본 이미지 드롭인: `src/assets/pages/{manual/1-neis, manual/2-signup, eduonepass/signup}/`에 `01.png…` 저장하면 자동 표시. 안내: `src/assets/pages/README.md`.
- [ ] 발행사 답변 12곳·FAQ 문서가 최신인지 확인(원본 갱신 시 `src/assets/replies/…` 교체, 스키마 `src/content/publishers/*.mdx`).

**P4 잔여 (기능 마감)**
- [ ] 사이트 내 검색(Pagefind) 도입 — 정적 인덱스, 무료. 헤더 🔍 자리에 연결.
- [ ] 접근성(KWCAG) 점검 — 대비·포커스·대체텍스트·landmark 최종 확인.
- [ ] 동료용 저작 양식/가이드 문서(P4 산출물) — `docs/`에 마크다운 템플릿 + 메타 항목표.
- [ ] (선택) 웹폰트 self-host 전환 — 현재 CDN(Pretendard/Hahmlet). 개인정보 원칙상 P4에서 전환 가능.

**보류·확인 사항**
- manual·eduonepass "가입 방법 영상"은 원본이 인증 걸린 구글 자산이라 자동 이관 불가 → 이미지 드롭인으로 대체하거나, 유튜브 원본이 있으면 `<YouTube id=…/>`로 임베드 가능.
- `docs/original/CONTENT.md`(원본 실측)·`replies-crawl.json`(이미지 인벤토리)는 재크롤 없이 참고용으로 보존.

---

## 현재 단계: 리디자인 — 모던 공공 포털

### 전면 리디자인 + 피드백 반영 (2026-07-11)
- **디자인 방향**: "구글 사이트 느낌" 탈피 → **모던 공공 포털**(딥 네이비 + 웜 오렌지 액센트). 토큰·레이아웃 문법은 `docs/DESIGN.md`에 재사용 가능하게 문서화.
- **토큰 교체**(`src/styles/global.css`): 스틸 슬레이트·파스텔 시안 → 네이비 스케일(primary-50~800) + `accent-soft`. 구 토큰명(`hero-*`, `band`)은 값만 바꿔 하위 페이지 호환 유지.
- **홈**: 중앙 정렬 히어로 → 비대칭 2열(좌 타이틀 / 우 연락처 전화 카드 — 구 연회색 밴드 흡수, 설명문은 "활용하여" 뒤 줄바꿈). 다크 밴드 2개 해체(절차 문구 → 섹션 헤딩, 문의 → 네이비 CTA 패널). Step 카드 → 번호 레일(01·02 + 연결선, 3번째는 "추천자료" 배지). UI 라벨 이모지 제거(본문 문구는 원본 유지).
- **공통**: 푸터 다크 네이비 + 우상단 교육청 로고(64px, 원본 webp가 이미 투명 배경), 헤더 활성 메뉴 필 스타일, `title-underline` 듀오톤 마크로 재정의(하위 페이지 자동 적용), LinkButton `rounded-lg`.
- **발행사 답변**: 정적 상세 페이지(`[id].astro`) 삭제 — 허브의 클라이언트 필터(아코디언 제자리 표시)로 단일화. 삭제된 페이지 참조 링크 없음 확인. ※ PowerShell `Remove-Item`은 `[id]`를 와일드카드로 해석하므로 `-LiteralPath` 필요.
- **콘텐츠 수정**: 금성출판사 회신 이미지 6번(`replies/kumsung/01/06.png`) 삭제(문서 5장). 수업설계 안내 문구(ℹ️/※) → info 아이콘 콜아웃 카드로 정리.
- 빌드 27페이지 정상(발행사 상세 12페이지 제거 반영), 콘솔 오류 0. 데스크톱·모바일 프리뷰 확인.

## 이전 단계: P3~P5 준비 — 핵심 완료

### 피드백 필터·이미지 슬롯·배포 준비 (2026-07-10)
- **발행사 답변**: "전체" 칩 제거, 초기 목록 숨김 → 발행사 이름 클릭 시 **페이지 이동 없이 클라이언트 필터**(칩 강조, #슬러그 딥링크, no-JS 폴백).
- **manual 빈 아코디언 제거**(빈 확장 UI 정리). 본문 텍스트·체크리스트·링크로 완결.
- **인증 걸린 원본 이미지 대응**: manual·eduonepass 단계 스크린샷은 학교 구글 계정 인증이 걸려 자동 이관 불가(403/CORS). → `PageImages` 컴포넌트 + 드롭 폴더(`src/assets/pages/{manual/1-neis,manual/2-signup,eduonepass/signup}/`) 배치. 운영자가 파일을 넣으면 자동 표시, 없으면 안내 플레이스홀더. 방법은 `src/assets/pages/README.md`.
- **배포 준비(P5)**: GitHub → Cloudflare Pages 방식 확정. `.nvmrc`(Node 20) + `docs/DEPLOY.md`(빌드 설정·커스텀 도메인 절차). 실제 레포 생성·CF 연결·DNS 전환은 운영자 계정 필요.
- 유튜브 영상(중등·수업설계)은 정상 렌더 확인.

## 이전 단계: P3~P4 (전 페이지 이관 + 마감)

### 접속 무결성 · 용어 변경 (2026-07-10)
- **용어 변경**: 사용자 대상 표기 "출판사" → **"발행사"**, 허브 제목 "출판사 피드백" → **"발행사 답변"** (헤더 메뉴·푸터·피드백/수업설계 페이지·404). 회사 고유명(금성출판사)·영문 코드 식별자(`publishers`)는 유지.
- **접속 안 되는 페이지 0 확인**: 빌드 산출물의 내부 링크를 전수 점검(broken=0). 헤더 "지원센터"가 가리키던 `/index`가 페이지 부재로 404였던 문제 → 원본과 동일하게 `/index` → `/` 정적 리다이렉트(astro.config) 추가.
- **404 페이지**(`src/pages/404.astro`): 잘못된 주소도 헤더 네비 + 주요 바로가기 제공(Cloudflare Pages가 dist/404.html 자동 사용).
- OG 메타·sitemap·favicon은 기존 반영. (검색(Pagefind)은 P4 잔여.)

### P3 완료 (2026-07-10)
- **일반 페이지 전량 이관** (원본 CONTENT.md 텍스트·구성요소 그대로):
  - `/index/manual` 사전 준비사항 안내(4단계 + 교육부 PDF·Canva·HWP 링크), `/index/AIDT` 활용 도움자료, `/index/eduonepass` 교육디지털원패스(서비스 6종), `/index/software` 학습지원 소프트웨어.
  - `/class` 수업이야기 허브(튜터링·교사/학생/학부모·웹전시관), `/class/curriculum` 수업설계 지원(KERIS·에듀나비 링크 + 단원/영역 목록 + 활용전략 영상), `/class/secondary` 중등 지원(유튜브 6편).
  - `/study` 스스로 학습 안내(원본 안내 포스터 이관 + 포털 바로가기 버튼), `/contact` 문의 및 장애 지원.
- **홈 FAQ**: GAS 임베드 → 정적 게시판 이관, `/index/faq`와 데이터 공유(src/data/faq.ts, 게시일 2026-05-03).
- **한글 경로 → 영문 slug** `public/_redirects` (수업이야기 하위 + 출판사 12곳).
- 외부 링크·유튜브는 유지(C6). 빌드 24페이지 정상, 콘솔 오류 0.

### 이미지 이관 참고 (Google Sites 토큰)
- googleusercontent 이미지 URL 토큰은 **페이지 로드마다 회전**하고 서명에 크기 파라미터가 포함됨 → 크롤 직후 즉시 다운로드해야 하며, DOM의 실제 `src`(크기 접미사 포함)를 그대로 사용해야 함(회신 이미지는 `=w1280`, 포스터는 원본 접미사).
- software 포스터는 carousel 지연 로드로 안정적 캡처 실패 → 텍스트+에듀집 링크로 충분(장식 이미지 생략). manual·eduonepass 본문 스크린샷도 텍스트가 내용을 담고 있어 생략.

## 이전 단계 기록: P2 (반복 콘텐츠 이관)

### P2 완료 (2026-07-10)
- **출판사 피드백 → QnA형 게시판(필터형)으로 재구성** (관계자 요청 반영):
  - 허브(`/index/feedback`): 원본 제목·안내문 + 출판사 필터 칩 + 전체 회신 목록(출판사 배지 + 교과 + 문서 수, 펼치면 문서 이미지).
  - 상세(`/index/feedback/{slug}`): `'{이름}' 회신 내용` 제목 + 해당 출판사만 목록형 필터 + ◀ 돌아가기. 필터는 정적 페이지 링크(JS 0줄).
  - **회신 문서 이미지 109장 전량 이관** → `src/assets/replies/{slug}/{섹션번호}/`. 빌드 시 WebP 최적화(예: 592KB→92KB). 원본 URL 인벤토리는 `scripts/download-replies.mjs`에 보존(서명상 `=w1280`만 유효).
- **`/index/faq` 페이지**: 원본 아코디언 3항목 → 같은 게시판 스타일, 문서 이미지 7장 이관.
- **홈 FAQ 임베드**: 원본 GAS(QnA 게시판) iframe 유지 임베드 — 프리뷰에서 정상 로드 확인.
- **로고·파비콘**: 원본 '교육수도 대구' 로고(2048px PNG) 확보 → 파비콘/애플터치 아이콘/헤더/히어로 반영 (`src/assets/logo.png`).
- **모바일 대응**: 헤더 햄버거 메뉴(`<details>` 기반, JS 0줄) + 게시판·그리드 반응형. 모바일(375px) 프리뷰 확인 완료.
- 빌드 15페이지 정상, 콘솔 오류 0.

### 남은 P2 잔여
- `resources` 컬렉션(매뉴얼·소프트웨어·도움자료 카드) 이관 — /index/manual·AIDT·software 페이지(P3)와 함께 진행이 효율적.

## 이전 단계 기록: P1 (골격 + 디자인 시안)

### 방향 전환 (2026-07-09 검수)
- **원본 충실 재현 원칙 확정**: 원본(구글 사이트)의 히어로 등 모든 텍스트는 사전 협의된 것 → 그대로 사용, 부가 텍스트·요소 삽입 최소화. 키 색상·메뉴 구조·메뉴 내 콘텐츠도 최대한 유지.
- 원본 전 페이지 실측 크롤링 완료 → `docs/original/CONTENT.md` (텍스트 전문, 네비 구조, 키 색상 `#263840`, 임베드 인벤토리).
- 초기 시안(딥 틸/코랄, 임의 히어로 문구, CTA 카드)은 원본 기준으로 전면 재작업.

### 완료
- Astro 5 + Tailwind 4 + MDX + sitemap 프로젝트 골격 (`npm run build` 정상, 15개 페이지 생성)
- 디자인 토큰 확정: 종이빛 배경 · 딥 틸(주색 `#0f7d84`) · 웜 코랄(강조 `#e2623b`), 제목 Hahmlet(한글 세리프) / 본문 Pretendard
- 재사용 컴포넌트 = 디자인 시스템: Header, Footer, PublisherCard, LinkButton, SectionHeading, **Accordion**(`<details>` 기반), **Carousel**(scroll-snap)
- 콘텐츠 컬렉션 스키마(publishers/pages/resources/faq) — zod 검증
- 시안 3종 렌더 확인:
  - 홈 (`/`) — 히어로 + 바로가기 카드 4 + 출판사 프리뷰
  - 출판사 허브 (`/index/feedback`) — 13개 카드 + 교과 칩
  - 출판사 상세 (`/index/feedback/[id]`) — 아코디언 + 유튜브 + CTA (천재교과서·비상교육 상세 본문 포함)

### 남은 P1 작업
- **Cloudflare Pages 연결** — GitHub 레포 생성 + CF Pages 프로젝트 연결(운영자 계정 필요). 빌드 명령 `astro build`, 출력 `dist/`.
- `*.pages.dev` 프리뷰 링크로 관계자 디자인 검수 → 색/타이포/컴포넌트 확정.

### 검수 결정 확정 (2026-07-09)
- 웹폰트: **CDN 유지, P4에서 self-host 전환**(콘텐츠 이관 먼저 마무리).
- 2depth 경로: **영문 slug 유지**(`/index/feedback/cheonjae`) — 기존 12개 데이터 파일과 일치, 추가 작업 없음.
- 다크 모드: **미지원, 라이트 전용 유지**.

## 다음 단계
- P2: `resources`·`faq` 이관 + 출판사 12곳 실제 링크/자료 채우기.
- P3: 일반 페이지(홈·안내·수업이야기·스스로학습·문의) 이관 + 구글 임베드 전수 점검.

## 알려진 참고사항
- 로컬 스크린샷 도구는 Pretendard 동적 서브셋(파일 ~100개)의 networkidle 대기로 간헐 지연됨 — 사이트 결함 아님. 실제 브라우저는 정상.
